# UAT Protocol — 交互式验收反馈流 (User Acceptance Testing Workflow)

## 原则

从 V2.0 起，执行 Agent 完成代码变更后，必须遵循以下四步交互式验收流程，**不得跳过用户手工测试环节**。

## 四步流程

```
┌─────────┐    ┌─────────┐    ┌──────────┐    ┌───────────┐
│ 1.改代码 │───▶│ 2.启服务 │───▶│ 3.出指南  │───▶│ 4.合并报告 │
└─────────┘    └─────────┘    └──────────┘    └───────────┘
                                      │               ▲
                                      ▼               │
                                 ┌─────────┐          │
                                 │ 用户测试 │──────────┘
                                 └─────────┘   (反馈)
```

### Step 1 — 完成代码编写/修改
- 修改或新增源代码文件
- 运行 `ruff check` + `mypy` 确保零报错

### Step 2 — 启动本地服务
- 执行 `python run.py` 或运行打包后的 `.exe`
- 确认控制台显示启动日志，浏览器已发起页面请求

### Step 3 — 输出《测试验收指南》并暂停
- Agent **必须主动停止**，将测试验收指南输出给用户
- 指南内容应覆盖: ① 控制台验证 ② 浏览器界面验证 ③ 核心功能操作验证
- 明确提示用户将测试结果反馈给 Agent

### Step 4 — 合并反馈生成最终报告
- 收到用户反馈后，将"执行详情"与"用户测试结果"合并
- 以标准 `[EXECUTION_REPORT]` 格式输出
- 若用户报告 Bug，Agent 应在报告中标注并等待后续任务包修复

## 执行纪律

| 规则 | 说明 |
|------|------|
| **禁止跳过 UAT** | 完成代码后不得直接关闭服务并生成报告 |
| **禁止合并 Step** | 不得在输出指南的同时擅自关闭服务 |
| **主动暂停** | Agent 输出测试指南后，必须等待用户明确回复 |
| **如实记录** | 用户反馈须原样记录在执行报告的"用户测试反馈"一栏 |

## 提交规范

所有执行报告必须在"工作详情"之后新增以下章节:

```markdown
## 5. 用户测试反馈 (UAT Feedback)
<用户测试结论原文 / 问题描述 / 截图引用>
```

---

*最后更新: 2026-05-11, V2.0 启动*
